name: Build Book

on:
  push:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build placeholder
        run: echo "Build workflow placeholder. Pandoc build steps will be added later."
