# Callout Boxes

This commit introduces the callout box system for the publishing engine.

The goal is to make recurring educational elements visually distinct:

- Key Idea
- Studio Mission
- Listening Exercise
- Deep Dive
- Avoid

Future build-system commits will connect `callout_renderer.py` directly to the Markdown parser so that callout blocks render as styled panels in the PDF.
