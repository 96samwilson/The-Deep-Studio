# SVG Rendering Engine

This commit begins direct SVG integration into the publishing engine.

Future implementation will use vector rendering so diagrams remain crisp at
all zoom levels and in printed editions.
