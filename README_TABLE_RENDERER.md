# Rendered Tables

This commit prepares the publishing engine to convert Markdown tables into
publication-quality ReportLab tables.

The next build integration commit will connect this renderer directly to
the Markdown parser.
