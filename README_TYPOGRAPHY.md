# Typography System

This commit establishes a single source of truth for book typography.

Future build scripts should import `scripts/typography.py` rather than
creating paragraph styles independently.
