# Figure Usage

Insert figures into Markdown using:

```text
{{figure:attention-cycle}}
```

The publishing pipeline will eventually:

1. Resolve the identifier.
2. Load the SVG.
3. Render it for PDF.
4. Add an automatic figure number.
5. Generate a caption and list of figures.
